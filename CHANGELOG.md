## 0.5.1

### Added

- Play button has a chunky 3D plate look that presses flat into the dock on hover and click.

### Changed

- Launcher window position and size now save inside `%APPDATA%\Vermeil` instead of a separate `%APPDATA%\com.vermeil.launcher` folder. Existing state migrates automatically on first launch.
