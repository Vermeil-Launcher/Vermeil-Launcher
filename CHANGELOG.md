## 0.5.7

### Fixed

- The pop-out logs window now matches the launcher's look with its own titlebar instead of the plain system window bar.
- When the game stops, the logs window closes and the logs return to the launcher's Logs tab automatically.
- The "Bring logs back" button in the Logs tab is now properly centered.
