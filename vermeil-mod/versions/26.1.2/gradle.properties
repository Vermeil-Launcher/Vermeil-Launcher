# Per-node dependencies for Minecraft 26.1.2 (Fabric). Same render-state cape
# hook as 26.2 (identical source compiles for both), so no version conditionals.
# No Fabric API — the mod uses only the loader + a client-tick Mixin.
minecraft_version=26.1.2
loader_version=0.19.3
java_version=25
